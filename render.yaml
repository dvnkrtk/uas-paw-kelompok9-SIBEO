services:
  - type: web
    name: uas-paw-kelompok9-SIBEO
    runtime: docker  # <-- TAMBAH INI
    dockerfilePath: Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        generateValue: true