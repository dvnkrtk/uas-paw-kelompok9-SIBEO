services:
  - type: web
    name: e-learning-platform
    env: python
    runtime: python-3.11
    buildCommand: pip install -r requirements.txt
    startCommand: pserve src/config/production.ini
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        generateValue: true
